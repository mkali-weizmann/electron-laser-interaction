results = tem.do_microscopy(img, protein, laser, camera, scope)
    exit_wave_op = get_exit_wave_op(sample, psi_in, scope, camera)
        ...
        elif sample['class'] == 'protein':
            atom_data = read_structure(sample)  # list of 3 dictionaries - of name, coordinate and id (atomic number) of the atoms
        potential = get_potential_from_atom_data(atom_data,
                                                protein_box_dims,
                                                pix_sizes)
            for atom in atom_data:
                get_atomic_potential(atom data)
    